Two chrome bars. AppHeader on tab-level screens (context = screen name in .3em caps). ScreenHeader on drill-ins (Tournament #12) with ‹ back.
```jsx
<AppHeader context="RANKINGS"/>
<ScreenHeader title="Tournament #12" caption="12 pairs · matchpoints" onBack={fn}/>
```