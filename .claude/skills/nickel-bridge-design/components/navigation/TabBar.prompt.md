Bottom tabs: CROSSINGS · STATS · RANKINGS (CROSSINGS = home: tournaments are crossings of the bridge). Active = inset 3px ink top bar + ink text; inactive = muted.
```jsx
<TabBar active="STATS" onSelect={setTab}/>
```