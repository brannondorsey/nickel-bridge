Brand marks. glyph = the workhorse (app header, inline notes); footer = multi-span colophon at screen bottoms; scene = the splash river vignette (splash/intro only).
```jsx
<BridgeMark width={26} /> <BridgeMark variant="footer" width={180} />
```