Bottom sheet: rgba(20,20,20,.34) scrim, white sheet with 2px ink top border, grab handle, Besley 800 title, ✕ close. Mount inside a relatively-positioned screen.
```jsx
<Dialog title="1NT — partner's response" onClose={close}>…</Dialog>
```