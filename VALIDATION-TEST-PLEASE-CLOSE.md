# Throwaway PR — confirming the gh-tools-allowlist fix posts a comment

This PR exists only to confirm that `.github/workflows/claude-pr-review.yml` (merged
in #63, granting `--allowedTools` for `gh pr view/diff/comment`) actually posts a
review comment now. Safe to close/delete this branch once confirmed — this file
isn't meant to be merged.
