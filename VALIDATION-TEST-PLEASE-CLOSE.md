# Throwaway PR — confirming the merged Claude PR Review workflow

This PR exists only to confirm that `.github/workflows/claude-pr-review.yml` (merged in #51,
now identical to `main`) actually posts a review comment on a newly opened PR. Safe to
close/delete this branch once confirmed — this file isn't meant to be merged.
